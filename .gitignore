# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.venv/
venv/
*.egg-info/
dist/
build/

# uv
.python-version

# Node
node_modules/
frontend/dist/

# Environment & secrets
.env
.env.local
.monarch_session
.mm/
# ...except the install-kit placeholder (comments only, no secrets) — it MUST ship in the kit
!install-kit/firemaster-starter/backend/.env

# Install kit: track the source folder, NOT the regenerable zip artifact
install-kit/*.zip

# Personal data — never tracked (copy config/properties.example.json to start)
config/properties.json
reports/
CLAUDE.local.md

# Logs
*.log

# Docker
docker-compose.override.yml

# IDE
.vscode/
.idea/
*.swp
*.swo

# OS
.DS_Store
.AppleDouble
.LSOverride
Thumbs.db

# Celery
celerybeat-schedule
celerybeat-schedule.db

# Testing
.pytest_cache/
.coverage
htmlcov/

# Jupyter
.ipynb_checkpoints/

# Claude Code
.claude/

# Playwright MCP
.playwright-mcp/

# Hosted-demo Caddy state (TLS certs, ACME account) — never commit
caddy/

# Playwright MCP debug artifacts (snapshots/console logs) — never commit
.playwright-mcp/
